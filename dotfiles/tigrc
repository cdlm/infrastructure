set line-graphics = yes
set main-view = \
    author:full \
    date:short,width=10 \
    id:yes \
    commit-title:yes,graph,refs \

bind generic [ !git fetch
bind generic ] !git push
bind generic { !git svnup
bind generic } !git svnci
